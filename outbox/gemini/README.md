# Gemini output

This folder holds the files that the LLM `google/gemini-3.1-flash-image-preview` generates from pairing each input image with the input prompt.
File name convention is to use identical names as the originals (so outbox/gemini, outbox/seedream and outbox/original should have the same file list (apart from png/jpg file extensions))
